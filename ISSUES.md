# Issues & Known Problems

> **Легенда:** ⏳ — связано с Fail2ban/Fail2ban+ (планируется рефакторинг, может стать неактуальным)

## Открытые проблемы

### 1. ⏳ Избыточное DEBUG-логирование в fail2ban_v2

**Файл:** `src/collectors/fail2ban_v2.py`
**Строки:** 283, 288

**Описание:**
При запуске в режиме DEBUG в логи попадает огромное количество записей вида:
```
DEBUG - [utm.fail2ban_v2] - Recorded unban: 142.93.128.113 from sshd
DEBUG - [utm.fail2ban_v2] - Recorded ban: 142.93.128.113 from sshd
```

**Причина:**
1. Каждое событие ban/unban логируется отдельно с уровнем DEBUG
2. Архивные `.gz` файлы парсятся заново при каждом запуске (строка 208 пропускает только для не-gz файлов)

**Возможные решения:**
- Убрать пофайловое логирование, оставить только итоговую статистику
- Добавить отслеживание позиции для .gz файлов (или флаг "уже обработан")
- Использовать более низкий уровень логирования (TRACE) для массовых сообщений

---

### 2. System Information виджет долго грузится при запуске

**Файлы:**
- `src/collectors/system.py` (метод `collect()`, строки 38-58)
- `src/dashboard/widgets/system_info.py`

**Описание:**
При запуске приложения виджет System Information долго загружается (до 30+ секунд).

**Причина:**
Все операции сбора данных выполняются последовательно в `collect()`:
| Метод | Max время | Кэш |
|-------|-----------|-----|
| `_get_package_stats()` | 25 сек | 30 мин |
| `_get_smart_info()` | 60 сек (10с × диск × 6 попыток) | 5 мин |
| `_get_service_stats()` | 4 сек | нет |
| `_get_disk_info()` + lsblk | 5 сек | нет |

Первый запуск особенно медленный — все кэши пустые. Быстрые данные (CPU, RAM) ждут завершения медленных (packages, SMART).

**Возможные решения:**
1. Разделить на "быструю" и "медленную" фазы сбора данных
2. Предзагрузка кэшей в фоне при старте приложения
3. Отложенная загрузка тяжёлых данных (через 5-10 сек после старта)
4. Параллельный сбор данных в отдельных threads
5. Показывать placeholder "Loading..." для медленных секций

---

### 3. ⏳ Table refresh failed: 'NoneType' object is not subscriptable

**Файл:** `src/dashboard/widgets/f2b_db_manage_modal.py`
**Строки:** 249-251

**Описание:**
При обновлении таблицы возникает ошибка:
```
ERROR - [utm.f2b_db_modal] - Table refresh failed: 'NoneType' object is not subscriptable
```

**Причина:**
Использование `dict.get(key, default)` не защищает от случая, когда ключ существует, но его значение равно `None`:
```python
geo = item.get("geo", {})       # Вернёт None, если geo=None в данных
geo.get("country", "?")         # None.get() → ошибка!
```

**Решение:**
Использовать `or` для защиты от None:
```python
geo = item.get("geo") or {}
attempts = item.get("attempts") or {}
bans = item.get("bans") or {}
```

---

### 5. Packages: "Update All" не обновляет все пакеты

**Файл:** `src/dashboard/widgets/packages.py`
**Строка:** 138

**Описание:**
После "U Update All" некоторые пакеты остаются необновлёнными (например, libnuma1, numactl, python3-distupgrade).

**Причина:**
1. Используется только `apt-get upgrade -y` без предварительного `apt update`
2. Индекс пакетов может быть устаревшим
3. Пакеты с новыми зависимостями могут быть "held back"

**Текущий код:**
```python
self.run_update_command([SUDO, APT_GET, "upgrade", "-y"])
```

**Решение:**
Выполнять `apt update` перед `apt upgrade`:
```python
# Вариант 1: одной командой через shell
self.run_update_command([SUDO, "sh", "-c", "apt-get update && apt-get upgrade -y"])

# Вариант 2: если held back пакеты всё ещё проблема — использовать dist-upgrade
self.run_update_command([SUDO, "sh", "-c", "apt-get update && apt-get dist-upgrade -y"])
```

**Примечание:** `dist-upgrade` более агрессивный — может удалить конфликтующие пакеты. Возможно стоит добавить предупреждение пользователю.

---

### 6. ⏳ Синхронные HTTP-запросы для геолокации IP

**Файл:** `src/collectors/fail2ban.py:480-493`
**Приоритет:** 🔴 Высокий
**Сложность:** Средняя

**Описание:**
Метод `_fetch_geo_data()` выполняет синхронный HTTP-запрос к ip-api.com с timeout=2 сек. При появлении 10 новых IP адресов это блокирует поток на 20 секунд.

**Текущий код:**
```python
def _fetch_geo_data(self, ip: str) -> Dict[str, str]:
    url = f"http://ip-api.com/json/{ip}?fields=country,org"
    with urllib.request.urlopen(req, timeout=2) as response:
        # ...
```

**Проблемы:**
- Нет батчинга запросов (ip-api поддерживает до 100 IP за запрос)
- Блокирует @work поток
- При недоступности API — 2 сек задержка на каждый IP

**Решения:**
1. Использовать batch API: `POST http://ip-api.com/batch` с массивом IP
2. Выполнять запросы в отдельном worker с очередью
3. Отложить загрузку геоданных до прокрутки пользователем (lazy load)

---

### 7. ⏳ O(n×m) сложность подсчёта атак в логах

**Файл:** `src/collectors/fail2ban.py:495-511`
**Приоритет:** 🔴 Высокий
**Сложность:** Средняя

**Описание:**
Метод `_count_attempts_from_logs()` для КАЖДОГО IP читает ВСЕ лог-файлы (включая .gz). При 50 IP и 10 MB логов = 500 MB чтения.

**Текущий код:**
```python
def _count_attempts_from_logs(self, ip: str) -> int:
    for log_file in glob.glob('/var/log/fail2ban.log*'):
        with opener(log_file, ...) as f:
            for line in f:
                if ip in line:
                    count += 1
```

**Проблемы:**
- Линейный поиск по всем логам для каждого IP
- Ротированные .gz файлы читаются каждый раз заново
- Нет кэширования результатов

**Решения:**
1. Построить индекс `{ip: count}` за один проход по всем логам
2. Кэшировать результаты парсинга .gz файлов (они не меняются)
3. Использовать mmap для быстрого поиска

---

### 9. ⏳ Множественные subprocess для fail2ban

**Файл:** `src/collectors/fail2ban.py:217-299`
**Приоритет:** 🟡 Средний
**Сложность:** Средняя

**Описание:**
Для каждого jail выполняются отдельные subprocess вызовы. При 5 jails = 10+ вызовов fail2ban-client.

**Текущие вызовы на jail:**
1. `fail2ban-client status <jail>` — информация
2. `fail2ban-client get <jail> bantime` — время бана

**Решения:**
1. Использовать `fail2ban-client -d` для дампа всей конфигурации за один вызов
2. Парсить sqlite базу fail2ban напрямую (`/var/lib/fail2ban/fail2ban.sqlite3`)
3. Кэшировать bantime (редко меняется)

---

### 10. ⏳ Неатомичная запись IP кэша

**Файл:** `src/collectors/fail2ban.py:62-68`
**Приоритет:** 🟡 Средний
**Сложность:** Низкая

**Описание:**
Метод `_save_ip_cache()` записывает JSON напрямую в файл без атомичности. При concurrent доступе или crash данные могут повредиться.

**Текущий код:**
```python
def _save_ip_cache(self) -> None:
    with open(BANS_DB_FILE, 'w', encoding='utf-8') as f:
        json.dump(self._ip_cache, f, indent=2)
```

**Сравнение:** `AttacksDatabase` использует RLock и атомичную запись (temp + rename).

**Решение:**
```python
def _save_ip_cache(self) -> None:
    tmp_path = BANS_DB_FILE + '.tmp'
    with open(tmp_path, 'w', encoding='utf-8') as f:
        json.dump(self._ip_cache, f, indent=2)
    os.replace(tmp_path, BANS_DB_FILE)  # Атомичная операция
```

---

---

### 15. ⏳ Чтение gzip логов без кэширования

**Файл:** `src/collectors/fail2ban.py:316-327`
**Приоритет:** 🟡 Средний
**Сложность:** Средняя

**Описание:**
Ротированные .gz логи (fail2ban.log.1.gz, .2.gz...) читаются полностью каждый раз при вызове `_get_recent_unbans()`. Эти файлы не меняются после ротации.

**Решения:**
1. Кэшировать результаты парсинга .gz файлов по имени/mtime
2. При обновлениях читать только fail2ban.log (текущий)
3. Хранить offset последней позиции чтения

---

### 19. ⏳ Нет debounce/cancellation для поиска

**Файлы:** виджеты fail2ban.py, logging.py (частично)
**Приоритет:** 🟢 Низкий
**Сложность:** Низкая

**Описание:**
При вводе в поле поиска таймер 0.3 сек сбрасывается, но если предыдущий поиск уже запущен в @work — он не отменяется.

**Решение:**
Добавить флаг/токен отмены предыдущего поиска:
```python
self._search_cancel_token += 1
current_token = self._search_cancel_token
# В worker:
if self._search_cancel_token != current_token:
    return  # Отменён
```

---

### 20. ⏳ Traefik target lookup — subprocess pipeline

**Файл:** `src/collectors/fail2ban.py:541-607`
**Приоритет:** 🟢 Низкий
**Сложность:** Средняя

**Описание:**
Метод `_get_traefik_target_for_ip()` создаёт pipeline из двух subprocess (grep | tail) для каждого IP. При 20 IP = 40 процессов.

**Решения:**
1. Читать лог один раз и строить map {ip: [targets]}
2. Использовать Python для фильтрации вместо grep
3. Кэшировать результаты парсинга access.log

---

---

### 22. ⏳ Fail2banPlusTab: lazy loading

**Файл:** `src/dashboard/widgets/fail2ban_plus.py`
**Приоритет:** 🟡 Средний
**Сложность:** Низкая

**Описание:**
Пропущен при рефакторинге #11. Требует перенос инициализации `AttacksDatabase` и `Fail2banClient` в `on_show()`.

---

---

## Сводная таблица новых проблем

| # | Проблема | Приоритет | Сложность | F2B |
|---|----------|-----------|-----------|:---:|
| 6 | Синхронные HTTP для геолокации | 🔴 Высокий | Средняя | ⏳ |
| 7 | O(n×m) парсинг логов | 🔴 Высокий | Средняя | ⏳ |
| ~~8~~ | ~~SMART блокирует update_data~~ | ✅ Решено | | |
| 9 | Много subprocess для fail2ban | 🟡 Средний | Средняя | ⏳ |
| 10 | Неатомичная запись кэша | 🟡 Средний | Низкая | ⏳ |
| ~~11~~ | ~~Все вкладки обновляются сразу~~ | ✅ Решено | | |
| ~~12~~ | ~~Refresh обновляет все виджеты~~ | ✅ Решено | | |
| ~~13~~ | ~~Дублирование сбора процессов~~ | ✅ Решено | | |
| ~~14~~ | ~~Два вызова systemctl~~ | ✅ Решено | | |
| 15 | Чтение gzip без кэша | 🟡 Средний | Средняя | ⏳ |
| ~~16~~ | ~~Export блокирует UI~~ | ✅ Решено | | |
| ~~17~~ | ~~Нет graceful degradation~~ | ✅ Решено | | |
| ~~18~~ | ~~Синхронная инициализация~~ | ✅ Решено | | |
| 19 | Нет debounce поиска | 🟢 Низкий | Низкая | ⏳ |
| 20 | Traefik subprocess pipeline | 🟢 Низкий | Средняя | ⏳ |
| ~~21~~ | ~~LoggingTab: preview + фоновая загрузка~~ | ✅ Решено | | |
| 22 | Fail2banPlusTab: lazy loading | 🟡 Средний | Низкая | ⏳ |
| ~~23~~ | ~~Автообновление версии в README~~ | ✅ Решено | | |

> ⏳ = связано с Fail2ban (планируется рефакторинг)

**Рекомендуемый порядок исправления (без F2B):** —

---

## Будущие реализации

### F1. Интеграционные и E2E тесты

**Приоритет:** Средний
**Сложность:** Средняя

**Описание:**
Текущее покрытие unit-тестами (43%) достаточно для критичных модулей. Интеграционные тесты проверят взаимодействие между компонентами.

**Что нужно протестировать:**

| Тип теста | Описание | Приоритет |
|-----------|----------|-----------|
| Collector → Widget | Данные корректно передаются и отображаются | Высокий |
| Config → Collectors | Настройки применяются правильно | Средний |
| Database → Collector | Чтение/запись attacks.db.json | Высокий |
| App lifecycle | Startup → Tab switch → Refresh → Shutdown | Средний |

**Инструменты:**
- `pytest-asyncio` — для асинхронных тестов Textual
- `textual.testing` — официальный тестовый фреймворк Textual
- Fixtures для mock-данных системы

**Пример интеграционного теста:**
```python
async def test_system_tab_displays_collector_data():
    app = UTMDashboard()
    async with app.run_test() as pilot:
        # Switch to System tab
        await pilot.press("0")
        # Verify data is displayed
        assert app.query_one(SystemInfoWidget).content != ""
```

**Текущий статус:**
Unit-тесты покрывают критичные модули на 80-100%. Интеграционные тесты отложены как менее приоритетные.

---

### F2. Рефакторинг архитектуры: слой сервисов

**Приоритет:** Низкий
**Сложность:** Высокая

**Описание:**
Текущая проблема: UI-виджеты напрямую вызывают методы коллекторов. Это создаёт сильную связность между View и Model.

**Текущая архитектура:**
```
Widget → Collector → System
```

**Предлагаемая архитектура:**
```
Widget → Service → Collector → System
```

**Преимущества слоя сервисов:**
- Бизнес-логика отделена от UI
- Легче тестировать (mock сервисов)
- Возможность добавить кэширование, валидацию, трансформацию данных
- Подготовка к API/CLI интерфейсам

**Пример:**
```python
# src/services/system_service.py
class SystemService:
    def __init__(self, collector: SystemCollector):
        self.collector = collector
        self._cache = {}

    def get_cpu_info(self) -> CPUInfo:
        data = self.collector.get_data()
        return CPUInfo(
            usage=data['cpu']['percent'],
            cores=data['cpu']['cores'],
            frequency=data['cpu']['freq']
        )
```

**Статус:** Отложено. Текущая архитектура работает стабильно.

---

### F3. Рефакторинг длинных методов

**Приоритет:** Низкий
**Сложность:** Средняя

**Описание:**
Некоторые методы превышают рекомендуемую длину в 50-100 строк.

**Частично выполнено:**
- ✅ `system.py:_get_disk_info` — разбит на 12 методов (коммит f410477)

**Оставшиеся кандидаты:**

| Файл | Метод | Строк | Рекомендация |
|------|-------|-------|--------------|
| ⏳ `fail2ban_v2.py` | `_parse_log_file()` | ~150 | Разбить на parse_line, handle_ban, handle_unban |
| `network.py` | `collect()` | ~120 | Вынести в отдельные _get_* методы |
| `services.py` | `_get_docker_containers()` | ~100 | Разбить по типам данных |

**Критерии рефакторинга:**
- Метод делает одну вещь
- Не более 3 уровней вложенности
- Легко понять без скроллинга

---

### F4. Миграция на pyroute2/systemd-python

**Приоритет:** Низкий
**Сложность:** Высокая

**Описание:**
Заменить subprocess-вызовы на нативные Python-библиотеки:
- `pyroute2` — для сетевых операций (вместо `ip`, `netstat`)
- `systemd-python` — для работы с systemd (вместо `systemctl`)

**Преимущества:**
- Нет зависимости от внешних утилит
- Типизированные данные вместо парсинга текста
- Лучшая производительность

**Недостатки:**
- Дополнительные зависимости
- Сложнее отлаживать
- Требует root для некоторых операций

---

### F5. Plugin-система для расширения функциональности

**Приоритет:** Низкий
**Сложность:** Высокая

**Описание:**
Возможность расширять UTM без изменения основного кода — пользовательские collectors и виджеты.

**Структура:**
```
src/plugins/
├── __init__.py          # Менеджер плагинов
├── base.py              # Базовый класс плагина
└── examples/
    ├── nvidia_gpu.py    # Мониторинг GPU
    ├── smart_health.py  # S.M.A.R.T. диски
    └── custom_alerts.py # Уведомления в Telegram
```

**Базовый класс:**
```python
class BasePlugin(ABC):
    name: str = "Plugin Name"
    version: str = "1.0.0"

    @abstractmethod
    def collect(self) -> Dict[str, Any]:
        """Сбор данных (как Collector)"""
        pass

    @abstractmethod
    def render(self, data: Dict) -> RenderableType:
        """Рендер виджета (Rich/Textual)"""
        pass

    def on_load(self) -> None:
        """Хук при загрузке"""
        pass
```

**Пример плагина (NVIDIA GPU):**
```python
# ~/.config/utm/plugins/nvidia_gpu.py
class NvidiaGPUPlugin(BasePlugin):
    name = "NVIDIA GPU Monitor"

    def collect(self):
        result = subprocess.run(
            ["/usr/bin/nvidia-smi", "--query-gpu=utilization.gpu,memory.used",
             "--format=csv,noheader"],
            capture_output=True, text=True
        )
        return {"gpu_util": result.stdout.strip()}

    def render(self, data):
        return Panel(f"GPU: {data['gpu_util']}", title="NVIDIA")
```

**Сложности реализации:**

| Аспект | Проблема |
|--------|----------|
| Изоляция | Плагины не должны ломать основное приложение |
| Безопасность | Выполнение стороннего кода — риск |
| API стабильность | Нужен версионированный API для плагинов |
| Зависимости | Каждый плагин может требовать свои пакеты |
| Hot-reload | Перезагрузка без рестарта приложения |

**Альтернатива (MVP):**
Конфигурируемые команды в `config.yaml` — покрывает 80% use-cases:
```yaml
custom_commands:
  - name: "GPU Temperature"
    command: ["/usr/bin/nvidia-smi", "--query-gpu=temperature.gpu", "--format=csv,noheader"]
    interval: 5
    parser: "single_value"
```

**Рекомендация:** Начать с конфигурируемых команд, полноценную plugin-систему реализовать при реальном спросе.

---

## Решённые проблемы

### ✅ 8. SMART данные блокируют update_data()

**Проблема:** `_get_smart_info()` блокировала `collect()` при устаревшем кэше (до 60 сек).

**Решение:** SMART сбор вынесен в фоновый поток + расширенный персистентный кэш `cache/disk_cache.json` с полями: device_type, model, serial, disk_type, transport, size_bytes, temperature, status.

---

### ✅ 11. Все вкладки обновляются сразу

**Проблема:** Каждый виджет в `on_mount()` запускал `update_data()`, даже если вкладка не видна.

**Решение:** Использование `on_show()` вместо `on_mount()` для загрузки данных. Флаг `_data_loaded` предотвращает повторную загрузку. LoggingTab останавливает таймер в `on_hide()`.

---

### ✅ 12. action_refresh() обновляет ВСЕ виджеты

**Проблема:** Ctrl+R обновлял все 11 вкладок, включая невидимые.

**Решение:** Обновлять только CompactSystemInfo + виджет активной вкладки через `TabbedContent.active`.

---

### ✅ 13. Дублирование сбора данных о процессах

**Проблема:** `SystemCollector._get_process_stats()` и `ProcessesCollector._get_processes()` оба вызывали `psutil.process_iter()`, дублируя работу.

**Решение:** Создан модуль `src/utils/process_cache.py` с shared cache для process_iter():
- `get_process_list(attrs)` — кэширует результаты на 2 сек
- `get_process_stats()` — возвращает total/zombies из кэша
- Оба коллектора используют общий кэш

---

### ✅ 18. Синхронная инициализация коллекторов

**Проблема:** Все 7 коллекторов создавались последовательно в `__init__()`, блокируя старт UI.

**Решение:** Ленивая инициализация через `@property` — коллекторы создаются при первом обращении.

---

### ✅ 4. Processes: "Running: 0" при первом открытии вкладки

**Решение:** Добавлен "прогрев" CPU счётчиков в `ProcessesCollector.__init__()`:
```python
def __init__(self, config=None):
    super().__init__(config)
    try:
        list(psutil.process_iter(['cpu_percent']))
    except Exception:
        pass
```

---

### ✅ 6. [Feature] Processes: сортировка по CPU% и клик по заголовкам

**Решение:**
1. По умолчанию сортировка по CPU% (descending)
2. Клик по заголовку колонки сортирует таблицу
3. Повторный клик меняет направление сортировки (asc/desc toggle)

Реализовано через `on_data_table_header_selected` в `ProcessesTab`.

---

### ✅ Processes: медленная загрузка из-за parent_name

**Проблема:** Для каждого процесса вызывался `psutil.Process(ppid).name()` — O(n²) операций.

**Решение:** Построение словаря `PID -> name` за один проход, затем O(1) lookup:
```python
pid_to_name = {}
for p in psutil.process_iter(attrs):
    pid_to_name[p_info['pid']] = p_info['name']
# ...
parent_name = pid_to_name.get(p_info['ppid'], '?')
```

---

### ✅ 14. Два вызова systemctl в _get_service_stats()

**Проблема:** Два отдельных вызова `systemctl` для получения failed и active сервисов.

**Решение:** Объединён в один вызов с `--all` флагом и подсчётом статусов в Python:
```python
result = subprocess.run([SYSTEMCTL, 'list-units', '--type=service', '--no-legend', '--all'], ...)
for line in result.stdout.splitlines():
    parts = line.split()
    if len(parts) >= 3:
        state = parts[2]  # ACTIVE column
        if state == 'failed':
            failed += 1
        elif state == 'active':
            active += 1
```

---

### ✅ 16. export_snapshot() блокирует UI

**Проблема:** Экспорт snapshot выполнялся на главном потоке, блокируя UI.

**Решение:** Обёрнут в `@work(thread=True)` с использованием `call_from_thread` для notify.

---

### ✅ 17. Нет graceful degradation для внешних сервисов

**Проблема:** При недоступности внешних инструментов (Docker, fail2ban) UI показывал пустые данные без объяснения.

**Решение:**
- Docker: добавлены статусы `not_installed`, `not_running`, `permission` в коллектор; ContainersTab показывает ошибку в header
- Fail2ban: добавлены error/error_type в коллектор; Fail2banTab показывает ошибку в header и таблице
- SMART: уже показывает "N/A" при недоступности данных

---

### ✅ 21. LoggingTab: быстрый предпросмотр + фоновая загрузка

**Проблема:** После перехода на lazy loading логи загружались полностью при открытии вкладки, вызывая задержку.

**Решение:**
- `_show_quick_preview()` — мгновенно показывает последние 50 строк (tail-like)
- `_load_full_logs_background()` — загружает все логи в фоне через `@work(thread=True)`
- Индикатор загрузки в border title: жёлтый мигающий ● → зелёный ● (follow on) / серый ○ (follow off)

---

### ✅ 23. Автообновление версии в README.md через release-please

**Проблема:** Версия в README.md обновлялась вручную.

**Решение:**
- Добавлен generic updater в `release-please-config.json`
- Добавлены маркеры `x-release-please-start-version` / `x-release-please-end-version` в README.md
- Версия теперь обновляется автоматически при релизе
