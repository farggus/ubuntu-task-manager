# Changelog

## v2.0.0 - Major Update: Полнофункциональный дашборд (2026-01-17)

### ⭐ Новые вкладки и виджеты

Дашборд расширен с 4 до **9 вкладок**:

| # | Вкладка | Описание |
|---|---------|----------|
| 1 | **Processes** | Мониторинг процессов с фильтрацией и управлением |
| 2 | **Services** | Systemd сервисы с управлением (start/stop/restart) |
| 3 | **Packages** | Установленные пакеты и обновления apt |
| 4 | **Containers** | Docker контейнеры с управлением и просмотром логов |
| 5 | **Tasks** | Cron jobs и Systemd timers |
| 6 | **Network** | Интерфейсы, порты, firewall, маршруты |
| 7 | **Users** | Активные сессии и все пользователи системы |
| 8 | **Disks** | Разделы дисков с иерархией |
| 0 | **Logging** | Live tail логов приложения |

### 🆕 Новые коллекторы

#### ProcessesCollector (`collectors/processes.py`)
- Полная информация о процессах: PID, имя, пользователь, статус
- CPU%, RAM%, память в MB
- Время запуска, полная команда, PPID, имя родительского процесса
- Статистика: всего, running, sleeping, zombies

#### UsersCollector (`collectors/users.py`)
- Активные сессии: пользователь, терминал, хост, время входа, длительность
- Все пользователи системы: UID, GID, shell, home, тип (system/human)

### 🔧 Расширенный SystemCollector

Добавлены новые метрики:
- **Температура CPU** (через psutil.sensors_temperatures)
- **Primary IP** и интерфейс
- **Статистика сервисов**: failed, active count
- **Статистика пакетов**: всего установлено, доступные обновления
- **Статистика процессов**: всего, zombies
- **Количество залогиненных пользователей**
- **Pretty OS name** (PRETTY_NAME из /etc/os-release)

### 📊 Улучшенная системная панель (CompactSystemInfo)

Теперь отображается в 3 колонки:

**Колонка 1 - Overview:**
- Hostname, IP, Kernel, Uptime
- Cores, Temperature
- Users, Processes (с зомби)
- Services (с failed), Packages (с updates)

**Колонка 2 - Resources History (Sparkline графики):**
- CPU Load + Load Average (60 точек истории)
- CPU Frequency
- RAM Usage (цветовая индикация: green <60%, yellow 60-80%, red >80%)
- SWAP Usage (green <20%, yellow 20-50%, red >50%)

**Колонка 3 - Disk Usage:**
- Таблица основных разделов (/, /boot, /home)
- Использовано/Всего и процент

### 🎮 Интерактивное управление (Hotkeys)

#### Processes Tab
- `A` - показать все процессы
- `Z` - показать только zombie
- `C` - отправить SIGCHLD родителю (очистка зомби)
- `K` - отправить SIGTERM родителю (убить зомби)

#### Services Tab
- `R` - restart сервиса (sudo systemctl restart)
- `S` - start сервиса
- `K` - stop сервиса

#### Packages Tab
- `A` - переключить все/только обновления
- `U` - обновить выбранный пакет
- `Shift+U` - обновить все пакеты
- `A-Z` - jump to letter

#### Containers Tab
- `A` - показать все контейнеры
- `R` - показать только running
- `S` - показать только stopped
- `X` - start контейнер
- `K` - stop контейнер
- `Shift+R` - restart контейнер
- `L` - показать логи контейнера (модальное окно)

#### Global Bindings
- `Ctrl+Q` - выход
- `Ctrl+R` - обновить все данные
- `Ctrl+S` - экспортировать snapshot в JSON
- `1-8, 0` - переключение между вкладками

### 🛠️ Утилиты и инфраструктура

#### Логирование (`utils/logger.py`)
- RotatingFileHandler (max 5MB, 3 backup)
- Формат: `%(asctime)s - %(levelname)s - [%(name)s] - %(message)s`
- Файл: `dashboard.log`
- Live tail в вкладке Logging

#### UI Helpers (`utils/ui_helpers.py`)
- `update_table_preserving_scroll()` - обновление таблицы с сохранением позиции
- `bytes_to_human_readable()` - конвертация байтов

#### Константы (`const.py`)
- APP_NAME, APP_VERSION, LOGGER_PREFIX, LOG_FILE

### 📦 Экспорт данных

**Ctrl+S** - экспорт текущего состояния всех коллекторов в JSON:
```json
{
  "timestamp": "2026-01-17T12:00:00",
  "hostname": "server",
  "system": {...},
  "services": {...},
  "network": {...},
  "tasks": {...},
  "processes": {...},
  "users": {...}
}
```

### 🎨 Улучшенные виджеты

#### ServicesCollector
- Добавлен **user** для каждого сервиса (через `ps -eo unit,user`)
- Улучшена обработка Docker: IP address, stack name (docker-compose)

#### ContainersTab
- IP адреса контейнеров
- Stack name (com.docker.compose.project label)
- Модальное окно для просмотра логов

#### DisksTab
- Иерархическое отображение: disk → partitions
- Summary строка для каждого физического диска
- Визуальная полоса заполнения: █░░░░

#### NetworkExtendedTab
- 4 под-вкладки: Interfaces, Ports, Firewall, Routes

#### TasksExtendedTab
- 2 под-вкладки: Cron Jobs, Systemd Timers

### 📁 Новая структура проекта

```
homeserver-inventory/
├── collectors/
│   ├── base.py
│   ├── system.py      # Расширен
│   ├── services.py    # Расширен
│   ├── network.py
│   ├── tasks.py
│   ├── processes.py   # НОВЫЙ
│   └── users.py       # НОВЫЙ
├── dashboard/
│   ├── app.py         # 9 вкладок
│   ├── style.tcss
│   └── widgets/
│       ├── system_info.py      # НОВЫЙ (Sparklines)
│       ├── services.py         # НОВЫЙ
│       ├── containers.py       # НОВЫЙ (с логами)
│       ├── processes.py        # НОВЫЙ
│       ├── users.py            # НОВЫЙ
│       ├── network.py          # НОВЫЙ (4 таба)
│       ├── tasks.py            # НОВЫЙ (2 таба)
│       ├── packages.py         # НОВЫЙ
│       ├── disks.py            # НОВЫЙ
│       ├── logging.py          # НОВЫЙ
│       └── container_log_modal.py  # НОВЫЙ
├── utils/
│   ├── logger.py      # НОВЫЙ
│   └── ui_helpers.py  # НОВЫЙ
├── const.py           # НОВЫЙ
└── tests/
    ├── test_services.py
    ├── test_tasks.py
    ├── test_integrity.py
    └── test_full_suite.py
```

### 🔄 Зависимости

Добавлен `pytest` в requirements.txt для тестирования.

### ⚠️ Требования

Некоторые функции требуют **sudo**:
- Открытые порты с процессами
- Firewall правила
- Системные cron файлы
- Управление сервисами (start/stop/restart)
- Управление контейнерами
- Apt upgrade

---

## v1.1.0 - Расширенный мониторинг Cron (2026-01-10)

### ⭐ Основные улучшения

#### Полный мониторинг cron задач

**Было:**
- Показывался только crontab текущего пользователя
- Простое отображение строк без парсинга
- Не было информации о следующем запуске
- Ограниченный просмотр системных задач

**Стало:**
- ✅ **ВСЕ crontab всех пользователей** системы
- ✅ **Все системные crontab** (/etc/crontab, /etc/cron.d/*)
- ✅ **Периодические задачи** (cron.hourly, daily, weekly, monthly)
- ✅ **Парсинг cron выражений** в человекочитаемый формат
  - `30 2 * * *` → "Daily at 2:30"
  - `*/5 * * * *` → "every 5 minutes"
  - `0 9 * * 1` → "at 9:00 on Mon"
- ✅ **Расчет следующего запуска**
  - Точное время: "2026-01-11 02:30:00"
  - Обратный отсчет: "in 5h 23m"
- ✅ **Детальная информация** по каждой задаче:
  - Пользователь
  - Источник (откуда взята задача)
  - Расписание (человекочитаемое)
  - Следующий запуск
  - Команда
  - Номер строки в файле

#### Новая команда list_tasks.py

Добавлена отдельная команда для просмотра **ВСЕХ** задач:

```bash
python list_tasks.py
# или с sudo для всех пользователей
sudo python list_tasks.py
```

### 🔧 Технические изменения

#### Новые зависимости
- `croniter>=2.0.0` - для парсинга cron выражений и расчета следующего запуска
- `python-dateutil>=2.8.0` - для работы с датами

#### Обновлены модули
- `collectors/tasks.py` - полностью переработан
- `dashboard/app.py` - улучшен TasksWidget

#### Новые файлы
- `list_tasks.py` - CLI утилита для просмотра всех задач
- `CRON_MONITORING.md` - подробная документация по мониторингу cron

---

## v1.0.0 - Первый релиз (2026-01-10)

### Возможности

- Системная информация (CPU, RAM, диски, uptime)
- Мониторинг сервисов (systemd, Docker)
- Сетевая информация (интерфейсы, порты, firewall)
- Базовый мониторинг cron и systemd timers
- TUI интерфейс на основе Textual
- Модульная архитектура коллекторов
- YAML конфигурация
